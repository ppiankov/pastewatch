public enum AppVersion {
    // WO-528@v2: version updates drive the guarded release workflow.
    // WO-529@v3: 0.35.0 packages opt-in ambiguous obfuscation.
    public static let current = "0.35.0"
}
